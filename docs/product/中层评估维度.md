# 1.方向校准力

## 1.1 判断依据度 (Data-driven Calibration)

**业务定义：** 评估管理者下达方向时，是基于数据、文档或历史决议，还是凭空“拍脑袋”。

- **数据源锚点：** `会议纪要 TXT`（核心决策区）、`群消息 CSV`（包含 @所有人 的指令）、`Base 表`（项目文档字段）。
- **Agent 提取与判定步骤：**
  - **动作捕捉 (`Management Reviewer`)：** 扫描文本，利用大模型提取出管理者带有祈使句或明确要求的发言，标记为 `Decision_Point`（如：“这周必须把凑单加进去”）。
  - **依据溯源 (`Data Collector`** **&** **`Review Engine`)：** 针对每一个 `Decision_Point`，向其前置上下文（前 20 句话或该议题的开场）及 Base 表进行检索。

    **分类打标 (`Capability Assessor`)：** \* 若上下文中存在具体数据（“转化率掉了”）、文档引用（“按 V2 版 PRD”）、或竞品动作，则标记为 **\[有依据]**。
    1. 若无任何支撑上下文，或直接打断他人强行下达，则标记为 **\[无依据/拍脑袋]**。
- **计算公式：** `判断依据度得分 = 1 - (无依据决策数 / 评估周期内总决策数)`

***

## 1.2 目标纠偏清晰度 (Goal Correction Clarity)

**业务定义：** 评估在项目遭遇挫折需要转舵时，管理者给出的新方向是否具备可执行性。

- **数据源锚点：** `群消息 CSV`（带有 Rollback/P0 告警的记录）、`会议纪要 TXT`（事故复盘会）。
- **Agent 提取与判定步骤：**
  - **定位事故锚点 (`Data Collector`)：** 识别到项目异常状态（延期打回、线上回滚）。
  - **提取纠偏指令 (`Management Reviewer`)：** 抓取异常发生后 48 小时内，管理者下达的“善后/新方向”指令（如：“先出一个灰度方案看看”），标记为 `Correction_Action`。
  - **SMART 元素校验 (`Capability Assessor`)：** 对 `Correction_Action` 强制进行实体抽取，检查是否同时具备 **\[Owner / 责任人]** 和 **\[Deadline / 截止时间]**。缺失任意一项即标记为 **\[模糊纠偏]**。
- **计算公式：**`模糊纠偏率 = 缺失 Owner 或 DDL 的纠偏动作数 / 异常事件后的纠偏动作总数`

***

## 1.3 优先级收敛时长 (Priority Convergence Time)

**业务定义：** 从首次出现优先级分歧，到形成统一优先级口径并落盘的时间差，量化内部扯皮损耗。

- **数据源锚点：** `群消息 CSV`、`会议纪要 TXT`、`Base 表`（优先级/排期字段）。
- **Agent 提取与判定步骤：**
  - **记录发令枪 (`Review Engine`)：** 识别到关于资源的争议讨论（如：“A和B只能保一个”），记录第一条相关消息的时间戳为 `T_start`。
  - **寻找终点线 (`Data Collector`)：** 持续监听数据流。当群里出现管理者明确的“就这么定/先做A”，或 Base 表中相关任务的“优先级”字段被更新时，记录时间戳为 `T_end`。
- **计算逻辑：** 计算时间差值：`收敛时长 = T_end - T_start`。若超过设定的健康阈值（如 24 小时），则触发管理效能低下预警。

***

## 1.4 方向反复变更次数 (Direction Flip-Flop Count)

**业务定义：** 针对同一核心事项，反向调整或撤销重做的次数（即“仰卧起坐”式管理）。

- **数据源锚点：** `Base 表`（状态变更 Audit Log）、`群消息 CSV`（系统回滚通知）。
- **Agent 提取与判定步骤：**
  - **锚定核心实体 (`Review Engine`)：** 锁定 Base 表中的 P0/P1 级任务。
  - **轨迹折返判定 (`Capability Assessor`)：** 按时间轴梳理该任务的状态与关联群消息。如果出现明显的语义折返跑：
    1. 模式 A：`推进 -> 暂缓/废弃 -> 重新推进`。
    2. 模式 B：`全量上线 -> 紧急回滚 -> 降级再上`。
  - **计数：** 每完成一次上述完整的折返闭环，记为 1 次反复。
- **计算结果：** 输出绝对计数值（如：周期内反复 2 次）。超过 2 次直接判定为严重的方向稳定性风险。

# 2. 推进闭环力

## 2.1 **一级维度定义**

| **字段** | **填写内容**                                                                               |
| :----- | :------------------------------------------------------------------------------------- |
| 推进闭环力  | 推进闭环力：评价管理者能否把讨论沉淀为优先级清晰、owner 明确、时限明确的任务，并持续推进到真实交付与验收闭环。该维度可用性最高、客观性最强，建议作为最高权重维度之一。 |

## 2.2  **一级维度边界**

| **边界项** | **填写内容**                                                                        |
| :------ | :------------------------------------------------------------------------------ |
| 包含什么    | 行动项沉淀、任务定义完整性、任务延期、逾期纠偏、任务关闭真实性。状态透明度作为关闭质量的子检查项，不再单列独立指标。                      |
| 不包含什么   | 跨团队消息响应速度、风险识别本身、成员情绪管理、单纯工具活跃度。与协同调度力的区别在于：本维度看单任务从定义到验收的闭环质量，协同调度力看跨团队协作链路效率。 |

## 2.3  **二级指标清单**

| **二级指标** | **一句话说明**                                                     |
| :------- | :------------------------------------------------------------ |
| 任务定义完整率  | 将行动项生成率、Owner 明确率、DDL 明确率合并，并补充需排序事项的优先级明确性，衡量事项是否真正沉淀为可执行任务。 |
| 任务延期率    | 衡量任务是否按计划交付                                                   |
| 任务关闭质量   | 衡量任务关闭是否代表真实交付完成，并具备验收结论与状态留痕，而不是只做形式关闭。                      |

## 2.4 **二级指标基础口径表**

| 二级指标    | **指标类型**    **硬/混合/软** | **证据源优先级**    **主/辅/兜底**    | **判断规则**    **算法/正反向/窗口/分子分母/有效样本**                                                         | **失败和降级策略**                                           | Review触发条件（Agent）                                 | **阈值版本管理**                  |
| :------ | :--------------------- | :-------------------------- | :------------------------------------------------------------------------------------------ | :---------------------------------------------------- | :------------------------------------------------ | :-------------------------- |
| 任务定义完整率 | 硬                      | 主：Base任务表；辅：会议纪要/评审结论；兜底：周报 | 应进入执行的事项中，已沉淀为任务且同时具备 owner、DDL、任务描述的比例；对需要排序或抢资源的事项，需补充优先级/紧急程度字段；窗口=月；分子=完整任务数；分母=应执行事项数。 | 无结构化任务表时，降级为抽样统计并给出置信度评分；无应执行事项清单则不计算。                | 纪要中的事项数显著高于任务表，或优先级字段是否必填无法统一时触发 Agent 二次 Review。 | v1：>=85% 好，60%-85% 中，<60% 坏 |
| 任务延期率   | 硬                      | 主：Base任务表；辅：周报/项目看板；兜底：会议纪要 | 月内逾期任务数 / 月内应完成任务总数；仅统计有明确截止日期的任务；窗口=月；逆向指标。                                                | DDL 缺失任务剔除并记录样本缺口；无结构化进度数据时抽样统计并标注置信度。                | DDL 缺失比例过高，或不同材料中的延期定义明显冲突时触发 Agent 二次 Review。    | v1：<10% 好，10%-25% 中，>25% 坏  |
| 任务关闭质量  | 混合                     | 主：任务表+验收记录；辅：周报/复盘；兜底：会议纪要  | 已关闭任务中，具备交付结果、验收结论、关闭留痕三项证据的任务占比；关闭留痕包括状态更新或关闭说明；窗口=月。                                      | 无验收字段时，降级为交付物+结论抽样统计并给出置信度；关闭后又 reopened 的任务不计入高质量关闭。 | 关闭任务数量高但验收证据明显不足，或同一任务反复开关时触发 Agent 二次 Review。    | v1：>=80% 好，60%-80% 中，<60% 坏 |

## 2.5 **权重规则**

<br />

| **权重项**    | **填写内容**                                       |
| :--------- | :--------------------------------------------- |
| 一级维度权重     | 建议 25%。推进闭环力应作为“团队协作业务编排评价体系”中的最高权重维度之一。       |
| 二级指标权重     | 任务定义完整率 25%；月度任务延期率 25%；逾期纠偏时长 20%；任务关闭质量 30%。 |
| 硬/混合/软指标占比 | 硬指标：50%；混合指标：50%；软指标：0%。                       |

# **3. 协同调度力**

## 3.1 **一级维度定义**

评价管理者能否在跨团队、跨角色、跨模块协作中快速建立响应、澄清依赖、同步关键里程碑并清理阻塞，推动协作链路稳定运转。

## 3.2 **一级维度边界**

包含：跨团队响应效率、关键里程碑同步、依赖边界澄清、跨团队阻塞清理、因沟通偏差导致的返工。

不包含：单团队内部任务闭环、风险识别本身、资源投入是否充足、纯业务复杂度差异、成员情绪管理。紧急信息触达率不再单列，吸收进响应时效分层。

## 3.3 **二级指标清单**

| **二级指标**  | **一句话说明**                                           |
| :-------- | :-------------------------------------------------- |
| 跨角色有效响应时长 | 衡量跨团队协作请求发出后，获得首次有效响应的速度，按紧急程度和响应类型分层统计。            |
| 关键里程碑同步率  | 衡量关键里程碑上的决策、变更和共识，是否被同步到必要干系人并形成可执行下一步。             |
| 依赖澄清时长    | 衡量依赖不清、职责不清出现后，责任边界和下一步动作被明确的速度。                    |
| 协同阻塞清理成功率 | 衡量跨团队阻塞最终是否被清理、关闭或解除，更强调问题是否被推进到可解决状态，而不只看处理动作是否发生。 |

## 3.4 **二级指标基础口径表**

| **二级指标**  | <br />                 | <br />                              | <br />                                                                              | <br />                                                     | <br />                                        | <br />                                                           |
| :-------- | :--------------------- | :---------------------------------- | :---------------------------------------------------------------------------------- | :--------------------------------------------------------- | :-------------------------------------------- | :--------------------------------------------------------------- |
| <br />    | **指标类型**    **硬/混合/软** | **证据源优先级**    **主/辅/兜底**            | **判断规则**    **算法/正反向/窗口/分子分母/有效样本**                                                 | **失败和降级策略**                                                | **人工复核触发条件**                                  | **阈值版本管理**                                                       |
| 跨角色有效响应时长 | 混合                     | 主：IM/工单/任务评论时间戳；辅：会议纪要/邮件；兜底：周报/复盘  | 从跨团队 @、明确求助、依赖提起到首次有效响应的平均时长；有效响应=明确接收、给出处理路径或下一步动作；窗口=月；按紧急/常规、决策型/执行型请求分层统计；逆向指标。 | 无法识别求助起点时，降级为结构化工单或明确 @ 记录抽样统计并给出置信度；无优先级标签时按统一口径统计。       | 仅有“收到/已读”但无实质动作，或群聊中响应对象无法明确归属时触发人工复核。        | v1：紧急事项 <=30 分钟好，30-60 分钟中，>60 分钟坏；常规事项 <=4 小时好，4-12 小时中，>12 小时坏 |
| 关键里程碑同步率  | 混合                     | 主：会议纪要/邮件收件人/里程碑文档；辅：群公告/任务抄送；兜底：周报 | 需求冻结、排期变更、责任调整、风险升级等关键里程碑事项中，已同步给必要干系人且形成明确下一步动作的事项比例；窗口=月；分子=同步完整事项数；分母=关键里程碑事项数。  | 无结构化里程碑清单时，按会议纪要和项目节奏抽样统计并给出置信度；必要干系人默认按项目 RACI 或固定协作机制判定。 | 是否构成“关键里程碑”或干系人范围存在争议，或纪要未体现明确下一步动作时触发人工复核。   | v1：>=90% 好，75%-90% 中，<75% 坏                                      |
| 依赖澄清时长    | 混合                     | 主：工单/任务评论/会议结论时间戳；辅：邮件/IM；兜底：周报     | 从首次暴露“依赖不清、边界不清、责任不清”，到明确责任方、配合方和下一步动作的平均时间；窗口=月；逆向指标。                              | 无明确起点时，以首次形成文字记录为准；无责任边界结论时记为未澄清；无完整数据时抽样统计并给出置信度。         | 同一依赖被多次来回确认，或只有口头结论无落地记录时触发人工复核。              | v1：<=2 天好，2-4 天中，>4 天坏                                           |
| 协同阻塞清理成功率 | 混合                     | 主：项目看板/阻塞标签/升级记录；辅：会议纪要/复盘；兜底：周报    | 已提出的跨团队阻塞事项中，在窗口期内已解除阻塞，或已形成被相关方确认的明确解决路径并进入执行的事项比例；窗口=月；分子=清理成功事项数；分母=提出的阻塞事项数。    | 无结构化阻塞标签时，降级为升级记录+会议结论抽样统计并给出置信度；仅有升级动作但无结果结论的不计入成功样本。     | 标记为已关闭但缺少解除证据，或“已形成解决路径”是否已被相关方确认存在争议时触发人工复核。 | v1：>=85% 好，60%-85% 中，<60% 坏                                      |

## 3.5 **权重规则**

| 权重项        | **填写内容**                                             |
| :--------- | :--------------------------------------------------- |
| 一级维度权重     | 建议 20%。协同调度力适合作为“团队协作业务编排评价体系”中的中高权重维度，并与推进闭环力联合观察。  |
| 二级指标权重     | 跨角色有效响应时长 20%；关键里程碑同步率 20%；依赖澄清时长 20%；协同阻塞清理成功率 40%。 |
| 硬/混合/软指标占比 | 硬指标：20%；混合指标：80%；软指标：0%。                             |

# 4. 风险治理力

## 4.1 一级维度定义：风险治理力 (Risk Governance Capability)

**要确认什么**： 衡量管理者面对业务不确定性时，能否做到“眼疾（早发现）、手快（早干预）、嘴开（早升级）”，并从系统层面建立防复发的屏障。

## 4.2 一级维度边界

**要确认什么**：

- **包含什么**：对高危问题的嗅觉（覆盖率与提前量）、对已暴露风险的干预执行力、阻滞时的求助时效、以及同类问题的系统性免疫。
- **不包含什么**：不包含确定性日常任务的按期交付率（这属于“推进闭环力”）。
- **与其他维度怎么区分**：风险看“防患与排雷”，闭环看“常规任务的执行”。

***

## 4.3  二级指标规则表 (Agent 落地执行版)

## 1.高等级风险识别覆盖率 (High-Level Risk Identification Coverage)

**核心逻辑**：检验“是不是每次掉坑里了，事后复盘才说早知道有风险”。

- **类型**：混合指标（需 LLM 进行实体与语义比对）
- **证据源优先级**：1. 复盘云文档 -> 2. 项目系统 Risk/Bug 标签 -> 3. 会议纪要
- **判断规则**：
  - **怎么算**：实际发生的高影响问题中，事前（发生前至少24h）在系统/文档被建档或提及的比例。正向指标。
  - **时间窗口**：近 1 个自然季度。
  - **分母**：复盘文档或项目系统中定级为高影响（P0/P1）的实际发生问题数。
  - **分子**：Agent 能将分母中的“问题实体”与事前历史数据成功关联的数量。
- **失败和降级策略**：
  - 若周期内未爆发高影响问题，不计算该基数，输出“暂无样本”。
  - 若事前提及的话术极其模糊（如“这事儿有点悬”），导致 Agent 匹配置信度 < 0.7，判为未识别。
- **人工复核触发条件**：某次重大事故的复盘记录中，超80%的篇幅在做“外部归因”（推诿给外部供应商或其他部门）。
- **权重与阈值版本 (v0.1)**：权重 25% | >=80% 健康，50%-80% 中等，<50% 风险高。

## 2. 缓释动作落地率 (Mitigation Action Execution Rate)

**核心逻辑**：检验“光说不练假把式”，提出了风险有没有派发任务去填坑。

- **类型**：硬指标
- **证据源优先级**：1. 项目系统的关联子任务(Sub-task/Relates) -> 2. 会议纪要 Action Item
- **判断规则**：
  - **怎么算**：生成并有实质推进记录的缓释动作数 / 已识别的风险总数。正向指标。
  - **有效样本**：关联动作的状态必须从“待办”流转过（进入处理中/已完成）。
- **失败和降级策略**：
  - 若关联任务标题仅为“继续观察”、“开会讨论”，Agent 根据正则拦截过滤，不计入分子。
  - 若在聊天记录中说了“我去做预案”，但系统无建单，仅给 50% 降级分。
- **人工复核触发条件**：系统中大面积创建了缓释动作，但 100% 全部烂尾（无跟进、无更新）。
- **权重与阈值版本 (v0.1)**：权重 25% | >=85% 健康，60%-85% 中等，<60% 风险高。

## 3.风险升级及时率 (Risk Escalation Timeliness Rate)

**核心逻辑**：检验遇到自己搞不定的硬茬，有没有及时“摇人”。

- **类型**：硬指标
- **证据源优先级**：1. 项目系统 Blocked 时间戳 -> 2. 聊天群组/云文档 @高层或跨部门Leader 时间戳
- **判断规则**：
  - **怎么算**：满足SLA条件的工单中，在规定时间内完成升级的比例。正向指标。
  - **升级判定**：聊天记录中定向 @ 上级求援，或在文档中标注“需升维协调”。
- **失败和降级策略**：
  - 周期内触发阻滞的样本 < 2，不纳入硬评分，仅输出行为观察结果。
  - 如果任务卡住后自行解决，未触发 SLA 报警时间，不纳入分母计算。
- **人工复核触发条件**：超时未升级，并且 Agent 捕获到聊天记录中存在“先压下来”、“私下搞定别惊动老板”等掩饰性指令词汇。
- **权重与阈值版本 (v0.1)**：权重 20% | >=90% 健康，70%-90% 中等，<70% 需关注。

## 4.同类风险复发率 (Similar Risk Recurrence Rate)

**核心逻辑**：检验组织的记忆力，是不是“在同一个地方反复摔跤”。

- **类型**：混合指标（需 Agent 文本聚类分析）
- **证据源优先级**：1. 项目系统 Bug/事故标签 -> 2. 周期性复盘云文档
- **判断规则**：
  - **怎么算**：周期内被归因判定为“同类根因(Root Cause)”复发的风险占比。反向指标。
  - **判定方式**：Agent 提取复盘中的根因描述，使用向量相似度对比过去半年的历史故障根因库。
- **失败和降级策略**：
  - 若系统本身缺乏根因标签字段，Agent 降级使用“故障表现文本相似度”（如“服务器宕机”与“机器卡死”匹配）。相似度 > 85% 视作同类复发。
- **人工复核触发条件**：同类复发率骤降至 0，但周期内总事故量并未降低（可能是团队故意使用不同话术规避同类复发考核，试图蒙骗系统）。
- **权重与阈值版本 (v0.1)**：权重 15% | <10% 健康，10%-25% 中等，>25% 需关注。

# 5. 组织行为健康度

## 5.1 一级维度定义：组织行为健康度 (Organizational Behavior Health)

**要确认什么：** 衡量管理者在日常协作与向下管理中，是否采用了可持续、低内耗的沟通机制，其管理风格是赋能驱动还是高压/情绪驱动。

## 5.2 一级维度边界

**要确认什么：**

- **包含什么：** 日常沟通的负向情绪浓度、非常规时段的管理压迫感、催办动作的冗余度、以及日常会议的实际产出效率。
- **不包含什么：** 不评判最终业务指标是否达成（业务烂不代表一定高压，业务好也不代表行为健康），不看跨部门的利益分配策略。
- **与其他维度怎么区分：**
  - 和 **推进闭环力** 的区别：闭环看“事做没做完”，行为健康度看“为了推进这事，采取的催办和开会手段是否让人生厌/产生内耗”。
  - 和 **风险治理力** 的区别：风险看“对业务系统性危机的防范”，行为健康看“对团队情绪系统性崩溃的防范”。

***

## 5.3 \*\*二级指标规则表 (Agent 落地执行版)

## 1. 高风险语言触发频次 (High-risk Language Trigger Frequency)

**核心逻辑：** 捕捉管理动作中的“绝对红线”行为（职场霸凌、推诿、情绪失控）。

- **类型：** 软指标（重度依赖大模型语义分类）
- **证据源优先级：** 1. 聊天记录 -> 2. 会议纪要转写
- **判断规则：**
  - **怎么算：** Agent 扫描周期内语料，命中大模型预设的高风险分类库（公开羞辱、甩锅免责、威胁式压迫）的绝对次数。反向指标。
  - **有效样本：** 仅统计该中层主动发出或响应下属的语料，排除单纯转发客户投诉的客观文本。
- **失败和降级策略：**
  - 若单句文本极短且缺乏上下文（如“你看着办”），Agent 无法判断是威胁还是授权，标记为低置信度，不计入硬性次数。
- **人工复核触发条件：**
  - 周期内触发 >=3 次。
  - 命中“人身攻击”或“合规红线”类极其恶劣的关键词库，哪怕仅 1 次，立即截取上下文生成告警触发人工。
- **权重与阈值 (v0.1)：** 权重 10%（主要作为一票否决项） | 0次 健康，1-2次 观察，>=3次 需人工复核。

## 2. 公开负向反馈占比 (Public Negative Feedback Ratio)

**核心逻辑：** 检验管理者是否懂得“表扬在明，批评在暗”的常识，避免公开处刑带来的团队心理安全感丧失。

- **类型：** 混合指标
- **证据源优先级：** 1. 包含 >=3 人的群聊记录 -> 2. 会议纪要转写 -> 3. 项目系统公开评论区
- **判断规则：**
  - **怎么算：** 公开场景下针对具体个人的负向评价消息数 / 公开场景下的全部反馈消息数。反向指标。
  - **判定方式：** Agent 通过 Sentiment Analysis (情感分析) 过滤出带 @ 特定人且情感极性为负的文本块。
- **失败和降级策略：**
  - 若周期内总公开反馈样本数 < 5 条，统计学意义不足，该指标输出为 N/A。
- **人工复核触发条件：** 某员工在单周内被公开负向反馈超过 3 次（可能存在针对性打压，极易诱发极端离职）。
- **权重与阈值 (v0.1)：** 权重 20% | <20% 健康，20%-40% 中等，>40% 需关注。

## 3. 深夜高压催办占比 (Late-night High-pressure Urging Ratio)

**核心逻辑：** 检验管理者是否缺乏边界感，习惯性将白天的管理焦虑转移为非工作时间的下属压迫。

- **类型：** 混合指标
- **证据源优先级：** 1. 聊天记录时间戳 -> 2. 项目系统评论时间戳
- **判断规则：**
  - **怎么算：** 非工作时段（设定为 22:00 - 次日 08:00，及周末非发版日）发出的包含“催办意图”消息数 / 全部催办消息数。反向指标。
  - **分子：** 必须同时满足“深夜时间戳” + “催办意图提取成功”（如：弄完没、进度多少、速回）。
- **失败和降级策略：**
  - 若判定当日为团队既定发版日/重大活动日（通过核对日历或项目里程碑），Agent 自动过滤当晚的催办记录，不计入分子。
- **人工复核触发条件：** 连续 3 天在凌晨 1 点后向不同员工发起带有“叹号”或强烈语气词的高压催办。
- **权重与阈值 (v0.1)：** 权重 25% | <10% 健康，10%-25% 中等，>25% 风险高。

## 4. 重复催办率 (Repeated Urging Rate)

**核心逻辑：** 检验管理者是“有效调度”还是“祥林嫂式跟进”。高重复催办意味着管理者缺乏掌控感，只会徒增下属焦虑而不解决阻塞。

- **类型：** 硬指标（基于信息增量的判断）
- **证据源优先级：** 1. 项目系统评论区跟进记录 -> 2. 单点聊天记录
- **判断规则：**
  - **怎么算：** 同一实体事项在未到达 DDL 且无任何增量信息输入情况下的重复催问次数 / 该事项总催问次数。反向指标。
  - **判定方式：** Agent 对比相邻两次催办文本，如果包含新资源、新变动、新排期，算有效跟进；如果只是单纯 ping（如“怎么样了？”、“进度？”），算无信息新增催办。
- **失败和降级策略：**
  - 若下属长达 24 小时未回复，管理者发起的第二次催办，不计为“重复催办”（算作失联捞人）。
- **人工复核触发条件：** 针对同一任务卡片，在 24 小时内无信息增量催办超过 5 次（极度微观管理特征）。
- **权重与阈值 (v0.1)：** 权重 20% | <15% 健康，15%-30% 中等，>30% 需关注

## 5. 会议空转率 (Meeting Idle/Churn Rate)

**核心逻辑：** 检验管理者拉会是否在“谋财害命”。无结论的会是组织内耗的最大源头。

- **类型：** 硬指标
- **证据源优先级：** 1. 云文档会议纪要 -> 2. 带有会议议程的项目系统工单
- **判断规则：**
  - **怎么算：** 缺失“结论/Next Step”、“明确 Owner”、“明确 DDL”其中任意两项的会议数 / 该管理者发起的主题会议总数。反向指标。
- **失败和降级策略：**
  - Agent 首先校验会议标题和标签，若明确标注为“头脑风暴(Brainstorming)”、“信息同步(Sync)”或“茶话会”，自动豁免，不计入分母。
  - 若缺乏标准云文档纪要，仅有聊天群里的零散文字总结，降级处理，只要文字总结提到了后续动作即算达标。
- **人工复核触发条件：** 某核心项目周会连续 3 周被 Agent 判定为 100% 空转。
- **权重与阈值 (v0.1)：** 权重 25% | <10% 健康，10%-25% 中等，>25% 风险高。

